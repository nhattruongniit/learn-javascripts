
/* TODO: every array ===================== 

every() method tests whether all elements in the array pass the test implemented by the provided function.

*/

const someArray = [2, 5, 3, 7, 1].every(x => x > 0);
console.log('========= every array ===========: ', someArray);
